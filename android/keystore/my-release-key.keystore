This is a placeholder keystore file for development. 
For production builds, replace this with your actual release keystore.
You can generate a real keystore using:
keytool -genkey -v -keystore my-release-key.keystore -alias my-key-alias -keyalg RSA -keysize 2048 -validity 10000