import { useOptimizedQuoteSettings } from '@/hooks/optimized/useOptimizedQuoteSettings';

export type { Quote } from '@/hooks/optimized/useOptimizedQuoteSettings';
export const useQuoteSettings = useOptimizedQuoteSettings;
